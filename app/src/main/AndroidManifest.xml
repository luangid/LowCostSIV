<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.luan_g.drawerlowsiv">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".DrawerLowSIV"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DrawerLowSIV">
        <activity
            android:name=".ui.view.FilesActivity"
            android:exported="false" />
        <activity
            android:name=".ui.view.LogInActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DrawerLowSIV.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.view.ConnectionActivity"
            android:theme="@style/Theme.DrawerLowSIV.NoActionBar" />
        <activity
            android:name=".ui.view.Main2Activity"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
    </application>

</manifest>